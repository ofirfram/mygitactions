name: Read artifact

on:
  workflow_dispatch:

jobs:
  read-artifact:
    runs-on: ubuntu-latest
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v2
        with:
          name: my-artifact
          path: artifact.zip
      - name: Read file
        run: |
          unzip artifact.zip
          cat file.txt
